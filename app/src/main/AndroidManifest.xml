<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lizhidu.lzopen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.TwoActivity"></activity>
        <activity android:name=".ui.activity.SimpleActivity"/>
        <activity android:name=".ui.activity.MoreActivity"/>
        <activity android:name=".ui.activity.NetworkActivity"/>
        <activity android:name=".ui.activity.BindingActivity1"/>


    </application>

</manifest>